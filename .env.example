# Copy to .env and fill in. NEVER commit .env.

# --- WordPress (read-only; Application Password, NOT the admin password) ---
WP_URL=https://example.com
WP_USERNAME=
WP_APP_PASSWORD=

# --- Google Search Console (OAuth "Desktop app" client) ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# URL-prefix property: https://example.com/   or Domain property: sc-domain:example.com
GSC_PROPERTY=https://example.com/
# Where the OAuth refresh token is cached (git-ignored)
GSC_TOKEN_PATH=tokens/gsc_token.json

# --- Obsidian Local REST API (OPTIONAL; vault is written directly as files) ---
OBSIDIAN_API_URL=https://127.0.0.1:27124
OBSIDIAN_API_KEY=

# --- Storage ---
DATABASE_PATH=data/seo.db
RAW_DATA_DIR=data/raw
OBSIDIAN_VAULT_PATH=obsidian/SEO-Knowledge-Graph

# --- Logging ---
LOG_LEVEL=INFO
LOG_DIR=data/logs

# --- AI Providers (OPTIONAL env fallback) ---
# Primary path: enter keys once in the AI Models UI → DPAPI SecretStore. These env vars are an OPTIONAL
# fallback for headless/server deploys; they are only read at call time, never stored or logged.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash

# --- SEO Brain API (FastAPI, Phase 2+) ---
# Optional bearer token required as X-API-Token by the API; the Next.js proxy injects it server-side (frontend/.env.local).
API_TOKEN=
# Monthly AI budget etc. are configured per site in the UI (site_settings); provider API keys are normally entered
# once in AI Models → stored DPAPI-encrypted by the SecretStore under data/secrets/ (git-ignored). The AI Providers
# section above is the only env exception (optional fallback for headless deploys).
