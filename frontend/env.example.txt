# SEO Brain frontend — copy to .env.local
# Backend base URL (FastAPI). Never expose the token to the browser: it is only read server-side
# by the proxy route app/api/backend/[...path]/route.ts and by server components.
SEO_BRAIN_API_URL=http://127.0.0.1:8000
SEO_BRAIN_API_TOKEN=
