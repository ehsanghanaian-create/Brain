"""Operator runbooks: prompts handed to a human or to an agent that has hosting access (DirectAdmin file manager +
phpMyAdmin), not to the AI Gateway. They live in the prompt library like every other prompt, so the text can be edited
in the platform, and `brain/ops/*` fills the derived values so the operator only supplies the two phone numbers."""

PHONE_CHANGE_TEMPLATE = """# تغییر شمارهٔ تماس سایت {{site}}

شمارهٔ تماس سایت را از {{old_phone}} به {{new_phone}} تغییر بده. این شماره در چند جا و چند قالب مختلف ذخیره شده؛
اگر همه را نگیری، بخشی از سایت (مخصوصاً هدر و دکمهٔ «تماس فوری») شمارهٔ قدیمی را نشان می‌دهد.

**قانون طلایی:** همیشه فقط بلوک ۸ رقمی را عوض کن و جداکنندهٔ بین دو نیمه را دست‌نخورده نگه دار
(داده‌های سریالایزشدهٔ وردپرس به طول رشته حساس‌اند؛ {{old8}} و {{new8}} هر دو {{len8}} رقم‌اند).

| حالت | قدیمی | جدید |
|---|---|---|
| لاتین، بدون جداکننده | {{old8}} | {{new8}} |
| لاتین، با فاصله | {{old8_sp}} | {{new8_sp}} |
| لاتین، با خط تیره | {{old8_dash}} | {{new8_dash}} |
| فارسی، بدون جداکننده | {{old8_fa}} | {{new8_fa}} |
| فارسی، با فاصله | {{old8_fa_sp}} | {{new8_fa_sp}} |
| فارسی، با خط تیره | {{old8_fa_dash}} | {{new8_fa_dash}} |
| فارسیِ اسکیپ‌شده (المنتور) | {{old8_esc}} | {{new8_esc}} |

## گام ۰ — پشتیبان (قبل از هر تغییر)

1. در phpMyAdmin از دیتابیس `{{db_name}}` خروجی SQL بگیر و فایل را نگه دار.
2. از فایل‌هایی که در گام ۱ ویرایش می‌کنی یک کپی با پسوند `.bak` بساز.
بدون این دو قدم جلو نرو؛ دستورهای گام ۲ تا ۴ برگشت‌پذیر نیستند.

## گام ۱ — فایل‌های افزونه/قالب (File Manager)

این فایل‌ها شماره را هاردکد دارند و دیتابیس آن‌ها را عوض نمی‌کند:

- `wp-content/mu-plugins/emdad-brand-optimizer.php` ← مهم‌ترین؛ پاپ‌آپ و دکمهٔ «تماس فوری» سایت‌واید (`emdad-phone`, `js-open-call`)
- `wp-content/mu-plugins/emdad-native-tel-tracking.php`
- هر لندینگ استاتیک زیر `public_html/` که `index.html` جدا دارد (مثل `emdad-renault/`, `emdad-asian-brands/`)

در هر فایل همهٔ هفت حالت جدول بالا را جایگزین کن، ذخیره کن، دوباره فایل را بخوان و تأیید کن که هیچ `{{old8}}` و
`{{old8_fa}}` باقی نمانده است.

## گام ۲ — دیتابیس وردپرس (phpMyAdmin)

نام دیتابیس و پیشوند جدول‌ها را از `wp-config.php` بردار (اینجا `{{db_name}}` با پیشوند `{{prefix}}`).
روی سه جدول زیر بزن — لاتین و فارسی، هر سه جداکننده:

```sql
{{sql_text}}
```

## گام ۳ — تلهٔ مهم: فارسیِ اسکیپ‌شده در دیتای المنتور

هدر و دکمه‌ها معمولاً در `{{prefix}}postmeta._elementor_data` به‌صورت یونیکد اسکیپ‌شدهٔ فارسی ذخیره می‌شوند، یعنی
`{{old8_fa}}` آنجا به شکل `{{old8_esc}}` نوشته شده است. جست‌وجو با رقم فارسیِ معمولی این را پیدا نمی‌کند (منفی کاذب)،
پس این فرم را جداگانه عوض کن:

```sql
{{sql_escaped}}
```

## گام ۴ — پاک‌کردن کش

```sql
{{sql_cache}}
```

بعد از آن: اگر افزونهٔ LiteSpeed Cache فعال است «Purge All» را بزن، وگرنه محتوای `wp-content/litespeed/` را خالی کن.

## گام ۵ — تأیید نهایی

۱. این شمارش‌ها باید همه صفر باشند:

```sql
{{sql_verify}}
```

۲. صفحهٔ زنده را با کش‌بایپس باز کن (`{{site_url}}?x=۱۲۳۴` یا Ctrl+F5) و چهار چیز را چشمی چک کن:
متن هدر، دکمهٔ «تماس فوری»، پاپ‌آپ تماس، و لینک `tel:` — همه باید {{new_phone}} باشند.
۳. اگر همین کار را برای چند سایت می‌خواهی، برای هر دامنه فایل‌ها و دیتابیس خودش را با پیشوند خودش تکرار کن.

## چک‌لیست کوتاه

- [ ] پشتیبان دیتابیس و فایل‌ها
- [ ] فایل‌ها: brand-optimizer + tel-tracking + لندینگ‌های استاتیک
- [ ] دیتابیس: posts / postmeta / options — شش حالت لاتین و فارسی
- [ ] فارسیِ اسکیپ‌شدهٔ `\\u06f…` در `_elementor_data`
- [ ] پاک‌کردن کش المنتور، ترنزینت‌ها و LiteSpeed
- [ ] تأیید با کش‌بایپس و شمارشِ صفر
"""
